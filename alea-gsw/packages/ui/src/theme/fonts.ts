import { Audiowide, Pathway_Extreme } from "next/font/google";

export const audiowide = Audiowide({
  weight: ["400"],
  subsets: ["latin"],
});

export const pathway = Pathway_Extreme({
  subsets: ["latin"],
});
